// Safe decimal math helpers for precision FX trading

export function parseUnits(_value: string, _decimals: number): bigint {
  // Parsing logic to be implemented (e.g. wrapper around standard bigints)
  return BigInt(0);
}

export function formatUnits(_value: bigint, _decimals: number): string {
  // Formatting logic to be implemented
  return '0.0';
}
